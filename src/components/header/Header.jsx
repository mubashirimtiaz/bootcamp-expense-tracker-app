import React from "react";
const Header = () => {
  return (
    <header className="text-center mt-2">
      <h1 className="text-muted">Expense Tracker</h1>
    </header>
  );
};

export default Header;
